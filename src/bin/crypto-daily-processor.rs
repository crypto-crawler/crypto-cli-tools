use crypto_msg_parser::{MarketType, MessageType};

fn process_files_of_day(
    exchange: &str,
    market_type: MarketType,
    msg_type: MessageType,
    day: &str,
    input_dir: &str,
    output_dir: &str,
) -> (i64, i64) {
    (0, 0)
}

fn main() {
    println!("Hello, world!");
}
